<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_product_template_form_rental" model="ir.ui.view">
        <field name="name">product.template.form.inherit.rental</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">

            <!-- On ajoute après le champ Sales Price -->
            <xpath expr="//field[@name='list_price']" position="after">
                <group string="Location">
                    <field name="rental_available"/>

                    <group invisible="not rental_available">
                        <field name="rental_price_hour"/>
                        <field name="rental_price_day"/>
                    </group>
                </group>
            </xpath>

        </field>
    </record>

</odoo>
